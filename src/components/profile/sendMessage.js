import React, { Component } from 'react';

class SendMessage extends Component {
    render() {
        return (
            <div>
                SEND MESSAGE
            </div>
        );
    }
}

export default SendMessage;